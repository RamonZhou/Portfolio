---
interface Props {
  rounded?: boolean;
}

const { rounded } = Astro.props;
---

<button data-ccursor="lift"
  class={`custom-btn text-xl max-h-[50px] active:shadow-none active:translate-x-[3px] active:translate-y-[3px] text-gray-200 px-5 py-2 border border-primary-200 hover:text-primary-200 transition-colors duration-100 ease-in-out bg-gray-900 cursor-pointer ${
    rounded ? "rounded-full" : "rounded-lg"
  }`}
>
  <slot />
</button>